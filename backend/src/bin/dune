; (executable
;  (name cli)
;  (modules cli)
;  (libraries core core_unix core_unix.sys_unix stdio game))

(executable
 (name server)
 (modules server)
 (libraries lib dream yojson ppx_yojson_conv core sexplib)
 (preprocess
  (pps ppx_jane ppx_yojson_conv)))

